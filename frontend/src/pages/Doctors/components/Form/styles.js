import colors from '../../../../styles/colors';
import styled from 'styled-components';

export const Error = styled.small`
        width:    100%;
        padding: 5px 0;

        font-weight: 300;
        font-size: .9rem;

        color: ${ colors.red };
    `;